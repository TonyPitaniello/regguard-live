/**
 * Soft PWA install prompt — removed. Download is one-click via header only.
 * Kept as empty export so old imports do not break.
 */
export default function PwaInstallBanner() {
  return null;
}
